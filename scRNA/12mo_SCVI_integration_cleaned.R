source([REDACTED_FILE_PATH]

run_SCVI_integration(object = [REDACTED_FILE_PATH] 
                     python_path = [REDACTED_FILE_PATH]
                     random_seed = 12345,
                     batch_col = "mouse",
                     project_path = [REDACTED_FILE_PATH]
                     RDS_path = [REDACTED_FILE_PATH]
                     use_GPU = TRUE,
                     hvgs = 2000,
                     n_layers = 2,
                     n_epochs = 400,
                     early_stopping = TRUE,
                     dropout_rate = 0.2)
